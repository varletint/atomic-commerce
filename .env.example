# API requests use the relative /api path, proxied by Vite (dev) or Vercel rewrites (prod).
# Only set this if you need to override the default proxy behaviour.
# VITE_API_BASE_URL=/api
VITE_APP_URL=http://localhost:5173
VITE_STRIPE_PUBLIC_KEY=
VITE_GA_ID=
